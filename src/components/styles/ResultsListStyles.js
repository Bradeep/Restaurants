import { StyleSheet } from 'react-native'
import Colors from '../../util/Colors'

const ResultsListStyles = StyleSheet.create({
    titleStyle: {
        fontSize: 18,
        fontWeight: 'bold',
        marginLeft: 15,
        color: Colors.BLACK
    },
    container: {
        marginBottom: 10
    }

})

export default ResultsListStyles