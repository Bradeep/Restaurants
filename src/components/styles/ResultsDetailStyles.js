import { StyleSheet } from 'react-native'

const ResultsDetailStyles = StyleSheet.create({
    container: {
        marginLeft: 15,
    },
    image: {
        width: 240,
        height: 120,
        borderRadius: 4,
        marginBottom: 5
    },
    name: {
        fontWeight: 'bold'
    }
})

export default ResultsDetailStyles